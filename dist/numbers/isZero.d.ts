
/**
* Checks to see if a value is zero
* @param val - the value to check
* @returns true if the value is zero
* @memberof vet.numbers
*/
declare function isZero(val : any): boolean;

export default isZero;
